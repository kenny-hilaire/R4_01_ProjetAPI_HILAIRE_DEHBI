RewriteEngine On
RewriteBase /API_FRONT_END/

# Servir les fichiers statiques (CSS, JS, images) directement
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Tout le reste passe par index.php
RewriteRule ^ index.php [L]
